{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "regions": ["bom1"],
  "rewrites": [
    {
      "source": "/match/:matchId/scorecard",
      "destination": "/live/:matchId"
    }
  ]
}
